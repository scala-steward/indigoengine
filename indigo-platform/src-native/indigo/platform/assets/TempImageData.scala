package indigo.platform.assets

/** The expectation is that this will be replaced with something else, once the SDL implementation takes shape.
  */
final case class TempImageData(width: Int, height: Int, data: Array[Byte])
